# kbsocket AI Agent 操作手册

## 1. 项目定位与架构红线

kbsocket 是下一代高性能用户态通信加速库，目标单容器支撑 40k 连接与 40k/s 建链。

在方案设计与实现中，必须遵守以下核心架构红线：
- **故障语义**：加速链路发生不可恢复错误时**显式失败**，由上层负责重连与决策。**严禁**在库内部实现跨 TCP 无损切换与复杂的 WR 数据重放。
- **传输模式**：跨机仅支持 raw URMA 的 `RM_CTP` 模式；同机提供 SHM 后端，NativeTCP 作为保底与兼容基线。
- **并发模型**：单 owner 推进物理队列，数据路径零争用（无全局互斥锁）。
- **依赖解耦**：核心库严禁反向依赖上层业务及 RPC 框架（如 bRPC），通过干净的 C ABI、标准 Socket 兼容层及现代 C++ 抽象（如 P2300 风格 sender/receiver）分层对外暴露。

## 2. 行为准则

在进行操作前需要确保下面的几点要求都被满足，不要求输出思维链，除非我明确要求。

- 遇到问题时不要只看表面，主动推断更深层的原因。为问题构造 1-3 个合理假设，并按可能性排序，然后验证最可能的假设，但是不要过早排除低概率但是高风险的可能。
- 在实现、分析过程中，如果新的信息否定原有的假设，需要更新假设集合，并相应调整方案。在多数情况下，你应优先尝试基于现有信息做出合理假设并推进，而不是因为细枝末节停滞不前。
- 代码是给人类阅读的，需要保证 **可读性**, **可维护性**。
- 只允许使用 mermaid 画示意图。
- 当进入 **Plan** 模式后，你需要再额外遵守 **Plan 模式行为准则** 章节的内容。
- **操作安全红线**：未经明确要求，严禁执行破坏性 Git 操作（如 `git reset --hard`、`git clean -fd` 等），不自动向远端仓库 `git push`。

## 3. Plan 模式行为准则

输入：用户的问题或任务描述。

在 Plan 模式中，你需要：

1. 自上而下分析问题，尽量找出根因和核心路径，而不是只对症状打补丁。
2. 明确列出关键决策点与权衡因素（接口设计、抽象边界、性能 vs 复杂度、锁与内存开销等）。
3. 给出 **1–3 个可行方案**，每个方案包含：
   * 概要思路；
   * 影响范围（涉及哪些模块 / 组件 / 接口）；
   * 优点与缺点；
   * 潜在风险；
   * 推荐的验证方式（应写哪些测试、跑哪些命令、观察哪些指标）。
4. 仅在 **缺失信息会阻碍继续推进或改变主要方案选择** 时，才提出澄清问题；
   * 避免为细节反复追问用户；
   * 若不得不做假设，需显式说明关键假设。
5. 避免给出本质相同的 Plan：
   * 如果新方案与上一版只有细节差异，只说明差异与新增内容即可。

## 4. 执行与验证准则 (Execution & Verification)

在 Plan 确认并进入编码/执行阶段时，必须遵守：

- **最小改动原则**：严格针对目标问题做精细化改动，严禁借机对无关文件做大面积格式重排或无关重构。
- **验证驱动**：
  - 修复 Bug 时，优先提供或运行能够复现该 Bug 的最小单元测试，绝不能仅凭“理论上修好了”就宣布完成。
  - 新增特性必须配套单元测试或 benchmark 校验。
- **环境解耦与 Mock**：
  - 单测与状态机逻辑应支持无真实 URMA 物理硬件环境运行（如 Mock Context、SHM 回环、NativeTCP 路径），确保在普通开发/CI 容器中可自测闭环。
- **错误处理风格**：
  - 严禁在核心库代码中随意调用 `exit()`、`abort()` 或抛出未捕获异常；
  - 统一通过显式错误码或 Result/Status 机制向上传递，所有失败分支必须由 RAII 保证资源妥善释放。

## 5. 高性能与系统级编程契约

- **数据路径零分配 (Zero Allocation)**：
  - 数据收发、轮询推进（poll）、CQE 处理等 Fastpath 上，**严禁**调用 `malloc`/`free`/`new`/`delete`，严禁产生容器隐式扩容（如 `vector::push_back` 触发 realloc）或 `std::string` 临时拷贝。
  - 统一使用预分配池化结构（如 RingBuffer、Object Pool、Buffer Lease）。
- **并发与缓存友善**：
  - Fastpath 避免互斥锁（Mutex）与阻塞式条件变量，优先使用单线程 Owner 或无锁环形队列推进。
  - 跨核/跨线程共享数据必须按 Cacheline 对齐（`alignas(64)`），杜绝伪共享（False Sharing）。
- **内存生命周期与 DMA 安全**：
  - 严禁向网卡投递未注册 DMA 内存（MR）；
  - 严格遵守 Buffer Lease 生命周期，在未获得本地/远端 CQE 完成事件前，底层内存绝不能提前释放或覆盖复用。
- **低开销可观测性**：
  - 动态跟踪优先使用 USDT (`DTRACE_PROBE` / SDT) 结合 eBPF，保证探针在未激活时仅为 NOP 指令，开销为零。
  - 严禁在 Fastpath 上硬编码未受门禁控制的字符串格式化、慢系统调用或高频日志输出。

## 6. 编码风格与提交规范

- **注释规范**：
  - 文档型注释使用 `///`, 普通注释使用 `//`, 禁用 C-style 的 `/* */`。
  - 在文档型注释中，关注函数、类的功能与设计取舍，而不是描述每个参数的意思，并且避免生成 doxygen 形式的注释，`@param`, `@code`, `@endcode`, `@ref` 等这种 tag。
- **语言使用分工**：
  - 代码、标识符、提交信息全部使用 **English**, 不能出现中文。
  - 注释、markdown 文档使用 **中文** 描述。
- **Git 提交格式**：
  - 遵循 Conventional Commits 格式：`<type>(<scope>): <summary>`（例如 `feat(rm_ctp): batch post recv wr`，`fix(shm): fix lease leak on peer abort`）。
